{
  "name": "@vigil/database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "lint": "eslint src/",
    "generate": "prisma generate",
    "push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vigil/config": "workspace:*",
    "eslint": "^9.0.0",
    "prisma": "^5.14.0",
    "typescript": "^5.0.0"
  }
}
